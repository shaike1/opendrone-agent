# Icebox

These are uncommitted possibilities, not ready work and not permission to implement.

- Deterministic in-memory simulator adapter and fault model (requires Phase 1 and hardened contracts).
- Plugin manifest schema and conformance suite (requires threat model and isolation decision).
- Audit-event schema and retention model (requires data classification and privacy review).
- Authenticated operator workflow (Phase 3 only).
- Hardware-in-the-loop vehicle adapter (Phase 4 only).
- Automated release provenance and SBOM publication (after license/release policy decisions).

Promote an idea only when it has a validated need, phase authority, owner, acceptance criteria,
dependencies, risk review, verification, and rollback.
